# Использование переводов (i18n) в проекте

## 🎯 Быстрый старт

Система переводов настроена и готова к использованию. Все переводы загружаются из файлов в `src/translations/`.

## 📝 Как использовать переводы в компонентах

### Пример использования хука `useTranslation`:

```tsx
"use client";

import { useTranslation } from "../hooks/useTranslation";

export default function MyComponent() {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t("landing.operators.web.hero.title")}</h1>
      <p>{t("landing.operators.web.hero.subtitle")}</p>
      <button>{t("landing.operators.web.hero.btn")}</button>
    </div>
  );
}
```

## 🔄 Переключение языка

Язык переключается автоматически через Header компонент. Выбранный язык сохраняется в `localStorage` и восстанавливается при следующей загрузке страницы.

### Поддерживаемые языки:

- **EN** - English (en.json)
- **ES** - Español (es.json)
- **PL** - Polish (pl.json)
- **RU** - Русский (ru.json)
- **UK** - Українська (uk.json)
- **ZH** - 中文 (zh-Hans-CN.json)

## 💾 Сохранение выбора

Язык автоматически сохраняется в `localStorage` с ключом `tada_language` и восстанавливается при следующем визите.

## 🔑 Формат ключей

Используйте точечную нотацию для ключей:

```json
{
  "landing.operators.web.hero.title": "Smarter Leasing. Better Tenants. Faster.",
  "landing.operators.web.hero.subtitle": "Renting has been broken...",
  "landing.operators.web.hero.btn": "Request Demo"
}
```

В компоненте:

```tsx
{
  t("landing.operators.web.hero.title");
}
```

## 📋 Доступные функции

### `useTranslation()` hook:

- `t(key: string)` - функция для получения перевода
- `language` - текущий код языка (en, es, pl, ru, uk, zh-Hans-CN)
- `isLoading` - флаг загрузки переводов

### `useI18n()` hook (продвинутый):

- `language` - текущий язык
- `translations` - объект со всеми переводами
- `setLanguage(lang)` - функция для изменения языка программно
- `t(key)` - функция перевода

## 🎨 Примеры для разных секций

### Для лендинга операторов:

```tsx
t("landing.operators.web.hero.title");
t("landing.operators.web.about.title");
t("landing.operators.web.contact.popup.btn");
```

### Для лендинга тенантов:

```tsx
t("landing.tenant.web.hero.title");
t("landing.tenant.web.rs.card1.title");
t("landing.tenant.web.gr.title");
```

## ⚠️ Важно

- Если ключ не найден, возвращается сам ключ (fallback)
- Все переводы загружаются асинхронно при смене языка
- Компоненты автоматически перерендериваются при смене языка
